$(document).ready(
					//Code that executes when the document is ready
					function(){
					    alert('The document is ready, sah!');
					  }
					);

$("body").click( function(){
    $("body").fadeOut (4000);
    $("body").fadeIn (4000);
    $("body").hide(4000);
    $("body").show(4000);